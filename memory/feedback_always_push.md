---
name: Always Push
description: User wants git push after every commit, no need to ask
type: feedback
---

Always push after committing. Don't ask, just do it.

**Why:** User expects changes to be on remote immediately.

**How to apply:** Every `git commit` should be followed by `git push` in the same step.
